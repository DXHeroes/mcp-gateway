# Profiles

A profile groups connections and defines which tools an AI client can use. Create a profile, assign connections and choose tool visibility and permissions. Use the profile's endpoint in the client. CE supports personal OAuth and signed-in sessions. Profile API keys require EE.

Sharing grants other organization members access according to their role. Review permissions before sharing. Removing a connection assignment restricts the profile without deleting the underlying service connection.
